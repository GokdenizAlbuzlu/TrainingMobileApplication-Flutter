Le dossier lib.zip contient le code source du projet

Pour executer le code source il est nécessaire d'installer flutter et toutes les dépendances installée pour le projet.

(Firebase, FlutterCharts ...)

Le plus simple est d'installer le .apk sur un emulateur android ou sur une machine android
